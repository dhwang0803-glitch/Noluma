# 세션 회고 — 2026-07-21 development (cache hash fix)

## 세션 요약
- 브랜치: development
- 커밋: 0건 (아직 미커밋)
- 변경 파일: 2개 (`BuildSummaryIndexUseCase.ts`, `RunInboxProcessUseCase.ts`)
- 교차 검증: Codex CLI 실행 완료 — P1 1건 수정, P2 1건 수용

## 배경
0.8.24(대칭 링크 강제) → 0.8.25(프롬프트 힌트 제거) → 0.8.26(전체 복원) 후에도
사용자의 Obsidian 노트 링크가 복원되지 않는 문제 조사. 사용자가 캐시 오염 가설 제기.

## 계획 vs 실제

| Phase | 계획 | 실제 | 일치 |
|-------|------|------|------|
| 캐시 흐름 조사 | 캐시가 링크 품질에 영향 주는지 분석 | contentHash 불일치 + onelineSummary 파괴 버그 발견 | 변경 |
| 캐시 버그 수정 | — | 해시 통일 + onelineSummary 보존 + 벡터 갱신 보장 구현 | 계획 외 |
| 실제 API 테스트 | — | Gemini 429 → OpenAI로 대체, 프롬프트 자체는 정상 확인 | 계획 외 |
| 교차 검증 | — | Codex P1(stale vector) 발견 → 즉시 수정 | 계획 외 |

## 패턴 분석

### Keep (유지)
- **근본 원인 추적**: 사용자 가설(캐시)을 따라 코드 흐름 전체를 추적, 해시 불일치 버그 발견
- **세 가지 수준의 수정**: root cause(해시 통일) + defense-in-depth(onelineSummary 보존) + P1 fix(벡터 갱신 보장)
- **실제 API 테스트**: 프롬프트가 정상 동작하는지 실제 LLM 호출로 검증
- **교차 검증 가치**: Claude가 놓친 stale vector 문제를 Codex가 발견

### Drop (중단)
- **다중 버전 빠른 릴리즈**: 0.8.24→25→26 세 번 연속 릴리즈가 문제를 복잡하게 만듦. 변경 후 충분한 테스트 없이 릴리즈
- **캐시 부작용 미고려**: 코드 변경(프롬프트/파서)만 복원하면 될 것으로 가정, 캐시 상태까지 고려 못함

### Try (시도)
- 프롬프트 변경 시 **캐시 무효화** 또는 **forceRebuild** 옵션 고려
- LLM 비결정성 대응: 동일 입력에 대한 결과 안정성 측정 도구

## 하네스 개선 제안
없음 (코드 버그 수정이므로 하네스 변경 불필요)

## 측정 지표
- 계획 이행률: 100% (조사 → 버그 발견 → 수정 → 검증 → 교차 검증 반영)
- 자기 편향 발생: 0회
- 아키텍처 드리프트: 없음
